<p id="notice"><%= notice %></p>

<h1>Changelogs</h1>

<table>
  <thead>
    <tr>
      <th>Dnsname</th>
      <th>Ipaddress</th>
      <th>Typeanswer</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @changelogs.each do |changelog| %>
      <tr>
        <td><%= changelog.dnsname %></td>
        <td><%= changelog.ipaddress %></td>
        <td><%= changelog.typeanswer %></td>
        <td><%= link_to 'Show', changelog %></td>
        <td><%= link_to 'Edit', edit_changelog_path(changelog) %></td>
        <td><%= link_to 'Destroy', changelog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Changelog', new_changelog_path %>
